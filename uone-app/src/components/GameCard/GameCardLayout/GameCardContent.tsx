import { CardContent } from "@material-ui/core";
import { FC } from "react";

const GameCardContent: FC = ({ children }) => {
  return <CardContent>{children}</CardContent>;
};

export default GameCardContent;
