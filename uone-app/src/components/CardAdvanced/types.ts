import React from "react";

interface IProps {
  children?: React.ReactNode;
  groupedAvatars: any;
  statusColor: string;
  title: string;
  subtitle: string;
  status: string;
}

export default IProps;
